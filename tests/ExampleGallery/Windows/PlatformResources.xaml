<!--  Windows platform resources; shared UI can reference these resources and get a Windows specific version -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExampleGallery">
    
    <Style x:Key="ExampleListGridStyle" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}"/>
        <Setter Property="Width" Value="300"/>
        <Setter Property="Height" Value="150"/>
        <Setter Property="Margin" Value="10,10,10,10"/>
    </Style>
    
    <Style x:Key="ExampleListTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource TitleTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="Margin" Value="20"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    
</ResourceDictionary>
